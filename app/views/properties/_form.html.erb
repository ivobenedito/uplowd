
<%= simple_form_for @property do |f| %>
  <%= f.error_notification %>

  <fieldset>
    <legend>Details</legend>

    <%= f.input :title %>
    <%= f.input :description %>
  </fieldset>

  <%= f.button :submit %>
<% end %>

<%= render 'photos' %>
<%= render 'components/upload_photos_dialog' %>
